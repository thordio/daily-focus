# Daily Focus — Demo Status

## What's Fixed (Code)

All code fixes are complete:

- **Capping**: Items capped at ~22-27 total across tabs; per-section minimums (AI Tech >= 4, AI Markets >= 6, Economy >= 6); score threshold >= 7.0
- **Prompts**: Rewritten for richer, more factual per-item content (background, key details, community discussion)
- **Validators**: Output validation enforces cap limits before write

## The Demo HTML Is Stale

The HTML files in `docs/daily/` were generated by the **old (pre-fix) code**. Current sizes:

| File | Items | Status |
|------|-------|--------|
| `2026-06-07-morning-zh.html` | 90 items | Pre-fix — well above capped target |
| `2026-06-07-morning-en.html` | ~90 items | Same |
| `2026-06-06-morning-zh.html` | 647 items | Pre-fix — unbounded |
| `2026-06-03-morning-zh.html` | 32 items | Old output |

The `index.html` still redirects to `2026-06-03-morning-zh.html`.

## To See Real Capped Output

Run the pipeline locally:

```bash
uv run horizon --hours 24 --period morning
```

Expected result: ~22-27 items total, each with rich factual enrichment
(background, key facts, community discussion, why-it-matters).

Or for Chinese output:

```bash
uv run horizon --hours 24 --period morning --language zh
```
